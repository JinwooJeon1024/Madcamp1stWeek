<!-- dialog_edit_restaurant.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <ImageView
        android:id="@+id/editRestaurantImage"
        android:layout_width="match_parent"
        android:layout_height="200dp" /> <!-- 예시 크기, 조정 필요 -->

    <EditText
        android:id="@+id/editRestaurantName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Restaurant Name" />

    <EditText
        android:id="@+id/editRestaurantAddress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Address" />

    <EditText
        android:id="@+id/editRestaurantPhoneNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Phone Number" />

    <!-- 이미지 URL을 수정할 EditText 추가할 수 있습니다 -->
    <!-- 기타 필요한 필드 추가 -->

</LinearLayout>
